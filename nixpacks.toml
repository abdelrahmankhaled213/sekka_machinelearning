[phases.setup]
aptPkgs = ["libgomp1"]

[phases.install]
cmds = ["python -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/pip install lightgbm"]

[variables]
PYTHON_VERSION = "3.11"